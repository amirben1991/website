export const environment = {
  production: true,
  // In production, use relative URLs if frontend and backend are on same origin
  // Otherwise, set the full URL to your production backend
  apiUrl: '/api',
  apiAuthUrl: '/api/auth',
};
